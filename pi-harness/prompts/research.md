---
description: Deep research persisted into an md writeup
argument-hint: "<topic> [outfile.md]"
---
Research the topic deeply: $ARGUMENTS

Use web search, primary sources, and the codebase as relevant. Prefer primary sources over summaries; cite them. Produce a structured markdown writeup: key findings first, then detail, then open questions. Write it to the given output file (default: `research/<slugified-topic>.md` in the repo, creating the directory if needed) so it survives the session. Give me a 5-bullet TLDR of findings when done.
