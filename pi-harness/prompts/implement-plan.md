---
description: Execute the current plan step-by-step, updating it as you go
---
Implement the plan now.

Rules:
- Work from the plan file in this repo (`plan.md` / `PLAN.md` / as I specify). If none exists, stop and ask.
- Execute steps in order. Step numbers only - never invent "phases".
- After EACH step: update the plan file marking that step done with a one-line note on what landed, then continue to the next step without waiting for me.
- Small changes at a time; verify each step compiles/builds before moving on.
- If a step turns out wrong or blocked: stop, update the plan with what you learned, ask one specific question.

$ARGUMENTS
